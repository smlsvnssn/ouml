import { describe, expect, it } from 'vitest'
import * as ö from '../ö.mjs'

describe('ö.equals', () => {
    it('should ...', () => {
        const result = ö.equals(1, 1)

        expect(result).toBe(true)
    })
})
