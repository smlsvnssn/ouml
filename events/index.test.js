import { describe, expect, it } from 'vitest'
import * as ö from '../ouml.mjs'
import * as övents from './index.mjs'

describe.todo(
    'hard to test layout events with jsdom. thinking about it',
    () => {
        it('should', () => {
            const result = 1
            expect(result).toBe(1)
        })
    },
)
